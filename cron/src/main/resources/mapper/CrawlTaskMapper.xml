<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- Jasmine code generator, a tool to build web crud application,with spring-
boot, mybatis, mysql,swagger,spring-security.
Generated at 2020-7-25 2:58:14 PM
All rights reserved by fuanlei(email:767550758@qq.com) since 2019 -->
<mapper namespace="com.jasmine.crawler.cron.mapper.CrawlTaskMapper">
    <insert id="add">
        insert into crawl_task
        (
         site_id,
         down_site_id,
        )values(
          #{siteId},
          #{downSiteId}
        )
    </insert>
    <update id="updateById">

    </update>
    <delete id="deleteById">
         delete from crawl_task where id = #{id}
    </delete>
    <select id="getTaskConfigToRun" resultType="com.jasmine.crawler.cron.pojo.config.CrawlTaskConfig">
      select
       *
      from crawl_task ct
      join down_system_site dss on ct.down_site_id = dss.id
      join down_system ds on dss.down_system_id =ds.id
      join site s on dss.site_id = s.id
      where
         ct.bind_status=1
      and ct.task_schedule_status=1
      order by ct.create_time asc
  </select>
    <select id="getTaskToBind" resultType="com.jasmine.crawler.cron.pojo.entity.CrawlTask">
      select
       *
      from crawl_task ct
      join down_system_site dss on ct.down_site_id = dss.id
      join down_system ds on dss.down_system_id =ds.id
      join site s on dss.site_id = s.id
      where
         ct.bind_status=1
      and ct.task_schedule_status=1
      order by ct.create_time asc
  </select>
    <select id="getById" resultType="com.jasmine.crawler.cron.pojo.entity.CrawlTask">
        select * from crawl_task where id =  #{id}
    </select>
</mapper>
